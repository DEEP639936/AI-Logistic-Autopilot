{
  "name": "fastapi-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Python FastAPI backend for AI Logistics Autopilot (drop-in replacement for the Next.js API routes)",
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --host 127.0.0.1 --port 8000",
    "start": "python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "seed": "python3 -m app.seed",
    "check": "python3 -m compileall app"
  }
}
